{% comment %}
  filename: sections/main-404.liquid
  purpose: Not-found state with massive negative space, high-stroke Playfair headline, and refined gold-outline return CTA.
  key variables: settings.brand_display_name, routes.root_url
  ai_agent_edit_guide: Keep structure here. Edit styling in assets/base.css and all copy in locales/en.default.json.
{% endcomment %}

<section class="section-shell scheme-light text-dark not-found not-found--spacious">
  <div class="page-width">
    <div class="not-found__content">
      <p class="not-found__brand">{{ settings.brand_display_name | upcase }}</p>
      <h1 class="not-found__heading">{{ 'page_404.heading' | t }}</h1>
      <div class="not-found__divider" aria-hidden="true"></div>
      <p class="not-found__subheading">{{ 'page_404.subheading' | t }}</p>
      <a class="button button--outline button--outline-gold" href="{{ routes.root_url }}">{{ 'page_404.button_label' | t }}</a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main 404",
  "settings": []
}
{% endschema %}

